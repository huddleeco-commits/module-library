{
  "name": "blink-backend-modules",
  "version": "1.0.0",
  "description": "Backend modules for the Blink platform",
  "main": "index.js",
  "scripts": {
    "test": "jest --config tests/jest.config.js",
    "test:unit": "jest --config tests/jest.config.js --testPathPattern=unit",
    "test:integration": "jest --config tests/jest.config.js --testPathPattern=integration",
    "test:watch": "jest --config tests/jest.config.js --watch",
    "test:coverage": "jest --config tests/jest.config.js --coverage",
    "test:verbose": "jest --config tests/jest.config.js --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "@types/jest": "^29.5.12"
  },
  "dependencies": {
    "express": "^4.18.2",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "ioredis": "^5.3.2",
    "stripe": "^14.14.0",
    "@anthropic-ai/sdk": "^0.71.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
