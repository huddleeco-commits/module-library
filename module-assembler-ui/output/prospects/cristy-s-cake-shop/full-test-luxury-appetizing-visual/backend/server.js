/**
 * full-test-luxury-appetizing-visual - Backend Server
 * Auto-generated by Module Library Assembler
 * 
 * Run: npm install && npm run dev
 */

const express = require('express');
const cors = require('cors');
const helmet = require('helmet');
const morgan = require('morgan');
require('dotenv').config();

const app = express();

// Trust proxy (required for Railway/Heroku/etc behind load balancer)
app.set('trust proxy', 1);

// ============================================
// MIDDLEWARE
// ============================================

// CORS - must be before other middleware
app.use(cors({
  origin: function(origin, callback) {
    // Allow requests with no origin (mobile apps, curl, etc)
    if (!origin) return callback(null, true);
    // Allow common deployment domains
    const allowedPatterns = [/\.be1st\.io$/, /\.be1st\.app$/, /\.railway\.app$/, /localhost/, /127\.0\.0\.1/];
    const isAllowed = allowedPatterns.some(p => p.test(origin));
    callback(null, isAllowed || true); // Allow all for flexibility
  },
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH', 'OPTIONS'],
  allowedHeaders: ['Content-Type', 'Authorization', 'X-Requested-With']
}));

// Handle preflight requests
app.options('*', cors());

app.use(helmet({ contentSecurityPolicy: false, crossOriginResourcePolicy: { policy: 'cross-origin' } }));
app.use(morgan('dev'));
app.use(express.json({ limit: '50mb' }));
app.use(express.urlencoded({ extended: true, limit: '50mb' }));

// ============================================
// MODULE ROUTES (Auto-Wired)
// ============================================

const authRoutes = require('./modules/auth/routes/auth.js');
const menuRoutes = require('./modules/menu/routes/menu.js');
const ordersRoutes = require('./modules/orders/routes/orders.js');
const stripe_paymentsRoutes = require('./modules/stripe-payments/routes/stripe.js');
const paymentsRoutes = require('./modules/payments/routes/payments.js');
const inventoryRoutes = require('./modules/inventory/routes/inventory.js');
const marketplaceRoutes = require('./modules/marketplace/routes/marketplace.js');
const vendor_systemRoutes = require('./modules/vendor-system/routes/vendor-sales.js');
const transfersRoutes = require('./modules/transfers/routes/transfer.js');
const notificationsRoutes = require('./modules/notifications/routes/notifications.js');

// Register routes
app.use('/api/auth', authRoutes);
app.use('/api/menu', menuRoutes);
app.use('/api/orders', ordersRoutes);
app.use('/api/payments', stripe_paymentsRoutes);
app.use('/api/billing', paymentsRoutes);
app.use('/api/inventory', inventoryRoutes);
app.use('/api/marketplace', marketplaceRoutes);
app.use('/api/vendors', vendor_systemRoutes);
app.use('/api/transfers', transfersRoutes);
app.use('/api/notifications', notificationsRoutes);

// ============================================
// BRAIN & HEALTH ROUTES (Admin System)
// ============================================

const brainRoutes = require('./routes/brain');
const healthRoutes = require('./routes/health');

app.use('/api/brain', brainRoutes);
app.use('/api/health', healthRoutes);

// Quick health check (legacy)
app.get('/health', (req, res) => {
  res.json({
    status: 'ok',
    project: 'full-test-luxury-appetizing-visual',
    timestamp: new Date().toISOString(),
    modules: 10
  });
});

// ============================================
// ERROR HANDLING
// ============================================

app.use((err, req, res, next) => {
  console.error('Error:', err.message);
  res.status(err.status || 500).json({
    error: err.message || 'Internal server error'
  });
});

// 404 handler
app.use((req, res) => {
  res.status(404).json({ error: 'Not found', path: req.path });
});

// ============================================
// START SERVER
// ============================================

const PORT = process.env.PORT || 5000;

app.listen(PORT, () => {
  console.log('');
  console.log('🚀 full-test-luxury-appetizing-visual backend running on port ' + PORT);
  console.log('   Health: http://localhost:' + PORT + '/health');
  console.log('   API:    http://localhost:' + PORT + '/api');
  console.log('');
  console.log('📦 Loaded modules (10):');
  console.log('   ✅ auth');
  console.log('   ✅ menu');
  console.log('   ✅ orders');
  console.log('   ✅ stripe-payments');
  console.log('   ✅ payments');
  console.log('   ✅ inventory');
  console.log('   ✅ marketplace');
  console.log('   ✅ vendor-system');
  console.log('   ✅ transfers');
  console.log('   ✅ notifications');
});

module.exports = app;
