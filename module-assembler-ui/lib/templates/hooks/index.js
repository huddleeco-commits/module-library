/**
 * Frontend Sync Hooks
 *
 * These hooks connect generated frontends to the admin API
 * for real-time data sync via Server-Sent Events (SSE).
 *
 * Copy these files to your generated project's src/hooks/ directory.
 */

export { useMenu } from './useMenu';
export { useReservations } from './useReservations';
